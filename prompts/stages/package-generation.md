# PACKAGE

Use only approved concept, script, and storyboard artifacts. Generate the final
production package and validate it against
`contracts/production-package.schema.json` plus runtime semantic checks. Wait
for package approval before asset handoff.
